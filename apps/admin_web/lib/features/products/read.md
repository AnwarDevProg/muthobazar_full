selecting attribute name will fill bn name and code, and selecting code will fill name en and bn.
selecting att value will fill the label bn
when click on save variation, it closes with dialog if has duplicate variation, but it should not close the dialog.


media selection is already croping image, but it should try to get full image inside the ration either full height or weight