name: shared_testkit
description: Shared testkit for MuthoBazar
publish_to: 'none'
version: 0.1.0

resolution: workspace
environment:
  sdk: ^3.9.0

dependencies:
  shared_models:
      path: ../shared_models

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8

