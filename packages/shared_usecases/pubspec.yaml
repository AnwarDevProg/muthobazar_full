name: shared_usecases
description: Shared usecases for MuthoBazar
publish_to: 'none'
version: 0.1.0

resolution: workspace
environment:
  sdk: ^3.9.0

dependencies:
  shared_models:
      path: ../shared_models
  shared_repositories:
      path: ../shared_repositories
  shared_workflows:
      path: ../shared_workflows

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8

