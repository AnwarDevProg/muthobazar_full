name: shared_models
description: Shared models for MuthoBazar
publish_to: 'none'
version: 0.1.0

resolution: workspace
environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8

