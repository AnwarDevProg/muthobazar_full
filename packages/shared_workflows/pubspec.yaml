name: shared_workflows
description: Shared workflows for MuthoBazar
publish_to: 'none'
version: 0.1.0

resolution: workspace
environment:
  sdk: ^3.9.0

dependencies:
  shared_models:
      path: ../shared_models
  shared_repositories:
      path: ../shared_repositories
  shared_services:
      path: ../shared_services

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8


