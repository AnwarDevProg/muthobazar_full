name: shared_ui
description: Shared UI package for MuthoBazar
publish_to: 'none'
version: 0.1.0

resolution: workspace
environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.6
  cached_network_image: ^3.4.1
  lottie: ^3.1.2
  smooth_page_indicator: ^2.0.1

  shared_core:
    path: ../shared_core
  shared_models:
    path: ../shared_models

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8

